# Vanilla JavaScript example

This example imports three individual engines from the generated
`@zutools/core` tarball. It does not use React or monorepo source paths.

From the repository root:

```bash
npm run example:vanilla
```

The preparation command rebuilds and packs Core, installs the tarball here and
starts Vite. Run `npm run examples:test` to build this example non-interactively.
